more `which isip_e` has help and usage parser for bash

look into specifying GPU devices and just how to get environment variables to run on GPUS. Look into PyTorch for CNNs. Pytorch has an env variable to run on GPUS we need to set. Cuda env variable

Histogram for labels processed

Train on a balanced set go through data and try and not just feed it backgroudn

Have 1000000 frames not background then train on 1000000 background

Debug QDA by making 2 separated datasets and putting them in the system

2 dimensional dct instead of 1 dimensional and serialize. Follow format of mpeg and jpeg.

Lay out series of experiments to tune base parameters so we have baseline:
dct_size, windowsize, framesize, model_type

Contemporary imaging structures like resnet, inception, ...
