for classify center function

get label information
shows where borders/ cooordinates are

return label info
    - could be zipped list of shapes and their coordinates (my preference)

frame = [n,m]
frame[0] = n
frame[1] = m


print_image.py calls classify_center

change arguments
    -> iname image name
    -> lname label name
    -> fsize frame size
    -> wsize window size

classify_center parameter: image_name, file_name

default frame size at the moment: -1 (total image)

if processed:
    return true
else:
    return false